
import 'package:flutter/material.dart';

class myTriggers extends StatelessWidget {
  const myTriggers({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}