
import 'package:flutter/material.dart';

class Schedulemedicine extends StatelessWidget {
  const Schedulemedicine({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}